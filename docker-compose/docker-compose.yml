services:
  catalog.db:
    image: postgres:17
  catalog.api:
    image: catalog.api
    build:
      context: ../
      dockerfile: src/services/Catalog/Dockerfile
  
  basket.db:
    image: postgres:17

volumes:
  postgres_catalog:
  postgres_basket: